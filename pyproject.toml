[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "timeless-forge"
version = "0.1.0"
description = "Reproducible wrapper for Forge AI-vs-AI simulation"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.setuptools]
package-dir = {"", "."}
packages = ["timeless_forge"]
